{
  "name": "liquid-audio-agent",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "install-python-deps": "node scripts/install-python-deps.js",
    "install-mcp-deps": "pip install -r requirements-mcp.txt",
    "verify-python-setup": "python setup_automation.py",
    "verify-mcp-setup": "python -c \"import liquid_audio, mcp; print('✅ MCP dependencies verified')\"",
    "start-mcp-server": "python mcp-server.py",
    "setup-all": "npm install && npm run install-python-deps && npm run install-mcp-deps",
    "dev-full": "npm run setup-all && npm run dev",
    "mcp-dev": "npm run install-mcp-deps && npm run start-mcp-server"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "@google/genai": "^1.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
